<div class="block-feedback">
  <span class="block-feedback__question">{{question-text}}</span>
  {{#each options}}
    {{#if answer}}
    <a href="{{answer.href}}" class="block-feedback__answer" title="{{answer.title}}">{{answer.text}}</a>
    {{/if}}
  {{/each}}
</div>
